ll=[1,2,3,'alex',True]
# 利用range打印出列表的索引
print('-------------range()---------')
for i in range(len(ll)):
    print(i)
#
#     + 类似于列表，自定制数字范围的数字列表。
#     + usage： r = range(100)
#     类似于r = [0, 1.txt, 2, ....99]
#     + 还有索引，切片
#     + range(start: end: step)也有反向的步长
# + range多半与for循环结合。
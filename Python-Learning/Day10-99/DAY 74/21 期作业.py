# 1.txt.完成一级菜单排序；
# 2.完成二级菜单默认选中并且展开的效果；
# 3.解决非菜单权限归属的问题（访问子权限，使得父权限的二级菜单是选中效果）；
# 4.完成路径导航功能；
# 5.以上功能完成后，总结各个功能实现时的表结构、构建的权限和菜单的数据结构。
# 例如：
# 一级菜单排序
# 表结构 添加了什么字段
# 权限的数据结构 permission_list = []
# 菜单的数据结构 menu_dict = {}
# 非菜单权限归属
# 表结构 添加了自关联
# 权限的数据结构 permission_list = []
# 菜单的数据结构 menu_dict = {}